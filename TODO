 * Implement, utilize chunks (for faster move between components_container s)
 * Component type id from compile time hash.
 * unique archetype_id + archetype_manager. Can be created only from main thread. ???
 * return std::span whenever possible,
 * move out components_container from impl + make container_view
 * add copyrights
 * container view
 * change query api to:
    world.query().select<data_x, data_y>();     // ??
    query(world).select<data_x, data_y>();
    query(world).contains<data_y>().select<data_x, data_y>();
    query(world).foreach_container();
    query(world).foreach([](data_x&, data_y&){});
 * move impl::utils -> impl